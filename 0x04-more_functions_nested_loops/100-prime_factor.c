#include <stdio.h>
#include <math.h>
/**
 * main - main block
 * Description: prime factor
 * Return: 0
 */
int main(void)
{
unsigned int long f = 612852475143, g = (int) sqrt(n);
	while (1)
	{
		if (f % g == 0)
		{
			printf("%lu \n", f / g);
			break;
		}
		g--;
	}
return (0);
}
