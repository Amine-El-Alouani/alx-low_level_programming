#include "main.h"

/**
 * _isdigit - check if it is a digit
 * @c: parametre to check
 * Deescription: check if the char is digit
 * Return: 0 or 1
 */

int _isdigit(int c)
{
	return (c >= 0 && c <= 9);
}
